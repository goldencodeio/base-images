# [MySQL](https://hub.docker.com/r/zolotoykod/mysql)

Custom docker image based on `[mysql:5.7](https://hub.docker.com/_/mysql)` and [setted up](./my.cnf) for Golden Code projects.

```bash
docker pull zolotoykod/mysql
```
